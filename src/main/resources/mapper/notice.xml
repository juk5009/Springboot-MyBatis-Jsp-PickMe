<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="shop.mtcoding.pickme.model.NoticeRepository">
    <select id="findAllWithCompany" resultType="shop.mtcoding.pickme.dto.notice.NoticeResp$NoticeMainRespDto">
        select nt.id, ct.company_name, nt.notice_noticename, nt.notice_title
        from notice_tb nt
        inner join company_tb ct
        on nt.company_id = ct.id
    </select>


    <select id="findByCompanyIdWithNotice" resultType="shop.mtcoding.pickme.dto.notice.NoticeReq$NoticeSaveReqDto">
        select nt.notice_noticename, nt.notice_title, nt.notice_career, nt.notice_pay, nt.notice_employtype, nt.notice_grade, nt.notice_location, nt.notice_content
        from company_tb tr 
        inner join notice_tb nt
        on tr.id = nt.company_id
        where nt.id = #{id}
    </select>

    <select id="findAll" resultType="shop.mtcoding.pickme.model.Notice">
        select * from notice_tb
    </select>

    <select id="findById" resultType="shop.mtcoding.pickme.model.Notice">
        select * from notice_tb where id = #{id}
    </select>   

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into notice_tb (company_id, notice_noticename, notice_title, notice_employtype, notice_location, notice_career, notice_grade, notice_pay, notice_content, created_at) values(#{companyId}, #{noticeNoticename}, #{noticeTitle}, #{noticeEmploytype}, #{noticeLocation}, #{noticeCareer}, #{noticeGrade}, #{noticePay},  #{noticeContent}, now())
    </insert>

    <delete id="deleteById" >
        delete from notice_tb where id = #{id} 
    </delete>

    <update id="updateById" >
        update notice_tb set notice_title= #{noticeTitle}, notice_employtype= #{noticeEmploytype}, notice_content= #{noticeContent}, notice_location= #{noticeLocation}, notice_career= #{noticeCareer}, notice_grade= #{noticeGrade}, notice_pay= #{noticePay} where id = #{id} 
    </update>
   
</mapper>