<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="shop.mtcoding.pickme.model.NoticeRepository">

    <select id="findAll" resultType="shop.mtcoding.pickme.model.Notice">
        select * from notice_tb
    </select>

    <select id="findById" resultType="shop.mtcoding.pickme.model.Notice">
        select * from notice_tb where id = #{id}
    </select>   

    <insert id="insert">
        insert into notice_tb (company_id, notice_title, notice_employtype, notice_content, notice_location, notice_career, notice_grade, notice_pay, created_at) values(#{companyId}, #{noticeTitle}, #{noticeEmploytype}, #{noticeContent}, #{noticeLocation}, #{noticeCareer}, #{noticeGrade}, #{noticePay}, now())
    </insert>

    <delete id="deleteById" >
        delete from notice_tb where id = #{id} 
    </delete>

    <update id="updateById" >
        update notice_tb set notice_title= #{noticeTitle}, notice_employtype= #{noticeEmploytype}, notice_content= #{noticeContent}, notice_location= #{noticeLocation}, notice_career= #{noticeCareer}, notice_grade= #{noticeGrade}, notice_pay= #{noticePay} where id = #{id} 
    </update>
   
</mapper>